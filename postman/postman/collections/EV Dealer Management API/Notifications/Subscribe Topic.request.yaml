$kind: http-request
url: "{{notificationServiceUrl}}/api/notification/subscribe-topic"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "deviceToken": "{{deviceToken}}",
      "topic": "{{topic}}"
    }
order: 2000
