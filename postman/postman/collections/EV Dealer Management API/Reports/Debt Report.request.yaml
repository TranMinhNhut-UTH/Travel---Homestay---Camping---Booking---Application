$kind: http-request
url: "{{reportingServiceUrl}}/api/reports/debt-report?dealerId={{dealerId}}&from={{reportFrom}}&to={{reportTo}}"
method: GET
queryParams:
  dealerId: "{{dealerId}}"
  from: "{{reportFrom}}"
  to: "{{reportTo}}"
order: 4000
