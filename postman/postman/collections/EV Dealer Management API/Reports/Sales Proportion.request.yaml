$kind: http-request
url: "{{reportingServiceUrl}}/api/reports/sales-proportion?from={{reportFrom}}&to={{reportTo}}"
method: GET
queryParams:
  from: "{{reportFrom}}"
  to: "{{reportTo}}"
order: 10000
