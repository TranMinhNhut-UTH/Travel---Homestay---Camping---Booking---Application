$kind: http-request
url: "{{reportingServiceUrl}}/api/reports/summary?type=sales&from={{reportFrom}}&to={{reportTo}}"
method: GET
queryParams:
  type: sales
  from: "{{reportFrom}}"
  to: "{{reportTo}}"
order: 8000
