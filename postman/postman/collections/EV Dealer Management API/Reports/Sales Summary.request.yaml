$kind: http-request
url: "{{reportingServiceUrl}}/api/reports/sales-summary?dealerId={{dealerId}}&fromDate={{reportFrom}}&toDate={{reportTo}}"
method: GET
queryParams:
  dealerId: "{{dealerId}}"
  fromDate: "{{reportFrom}}"
  toDate: "{{reportTo}}"
order: 13000
